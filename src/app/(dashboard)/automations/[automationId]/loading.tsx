export default function LoadingAutomationWorkflow() {
  return (
    <div className="fixed inset-0 z-[100] grid h-dvh w-dvw place-items-center bg-ronda-surface">
      <div className="text-sm font-semibold text-ronda-muted">Cargando workflow...</div>
    </div>
  );
}
